<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HealthyHeartTips">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/healthHeartTips_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:title="@string/healthy_heart_tips"
        app:titleTextColor="@color/colorWhite"
        app:titleTextAppearance="@style/TextAppearance.AppCompat.Medium"
        android:background="@color/colorToolbarBackground"
        android:theme="@style/AppTheme.AppBarOverlay"/>


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/heartTipsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/healthHeartTips_toolbar"/>


    <ProgressBar
        android:id="@+id/progressBar_HeartTips"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="invisible"/>


    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/heartTips_fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_add_sign"
            android:background="@color/colorLightBlue"
            app:rippleColor="@color/colorBlack"
            app:fabSize="mini"
            android:layout_gravity="bottom|end"
            android:layout_margin="30dp"/>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</RelativeLayout>